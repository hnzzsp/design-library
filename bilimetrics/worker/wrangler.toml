name = "bilimetrics-comments"
main = "worker.js"
compatibility_date = "2024-09-23"

# KV 命名空间：缓存评论（默认 10 分钟，避免每次请求都打 B 站，免费额度友好）
# 部署前先建：wrangler kv namespace create bilimetrics_comments
# 然后把下面 id 换成命令输出的 id
[[kv_namespaces]]
binding = "COMMENTS_KV"
id = "REPLACE_WITH_YOUR_KV_NAMESPACE_ID"
